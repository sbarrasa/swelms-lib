locale_id = en

textsByModule {
  default {
    GENERAL        = "This is a general message"
    TEST           = "Test"
    NOT_IMPLEMENTED = "Not yet implemented"
  }

  stock {
    NEAR_EXPIRATION = "The product {1} is about to expire in {2} days"
  }

  "kotlin.ranges.IntRange" {
    OUT_OF_RANGE = "The value must be between {1} and {2}"
  }
}
